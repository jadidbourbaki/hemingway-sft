# Python venvs and caches
.venv/
__pycache__/
*.pyc
.ruff_cache/
.ty_cache/
.pytest_cache/

# Fetched corpus and derived datasets, all reproducible from `just corpus`
data/

# Training output
runs/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Direnv / local env
.envrc
.env
.env.local

# Terraform state and provider binaries. The lock file is committed, the same
# way uv.lock is, so provider versions and checksums are pinned.
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
